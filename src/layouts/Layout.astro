---
export interface Props {
  title: string;
  description: string;
}
const { title, description } = Astro.props;
const canonicalPath = Astro.url.pathname;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://intheblack.coffee" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={`https://intheblack.coffee${canonicalPath}`} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=DM+Sans:wght@300;400;500;700&family=JetBrains+Mono:wght@400;500&family=Playfair+Display:wght@700;900&display=swap" rel="stylesheet" />
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "Organization",
          "@id": "https://intheblack.coffee/#organization",
          "name": "In The Black Coffee Co.",
          "url": "https://intheblack.coffee",
          "description": "Commission-only wholesale coffee sales for specialty coffee roasters",
          "areaServed": "United States"
        },
        {
          "@type": "LocalBusiness",
          "@id": "https://intheblack.coffee/#business",
          "name": "In The Black Coffee Co.",
          "description": "Commission-only wholesale coffee sales representative for specialty roasters",
          "url": "https://intheblack.coffee",
          "priceRange": "Commission-only"
        },
        {
          "@type": "Service",
          "@id": "https://intheblack.coffee/#service",
          "name": "Commission-Only Coffee Sales",
          "provider": { "@id": "https://intheblack.coffee/#organization" },
          "serviceType": "Wholesale Coffee Sales Representation",
          "description": "We sell your coffee wholesale to restaurants, offices, and specialty retailers on a pure commission basis — no retainer, no upfront cost."
        }
      ]
    }
    </script>
    <style>
      @keyframes fade-up {
        from { opacity: 0; transform: translateY(16px); }
        to { opacity: 1; transform: translateY(0); }
      }
      .fade-up { animation: fade-up 0.8s ease-out forwards; }
      .fade-up-2 { animation: fade-up 0.8s ease-out 0.2s forwards; opacity: 0; }
      .fade-up-3 { animation: fade-up 0.8s ease-out 0.4s forwards; opacity: 0; }
      .gold-line {
        background: linear-gradient(90deg, transparent, #C9A84C, transparent);
      }
      .gold-text {
        background: linear-gradient(135deg, #C9A84C, #E8C96D, #C9A84C);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Billy wordmark (partnership section) */
      .billy-wordmark {
        display: flex;
        align-items: baseline;
        line-height: 1;
      }
      .billy-mark-text {
        font-family: 'Playfair Display', serif;
        font-weight: 900;
        font-size: 42px;
        color: #FAF7F2;
        letter-spacing: -0.01em;
      }
      .billy-mark-dot {
        font-family: 'Playfair Display', serif;
        font-weight: 900;
        font-size: 42px;
        background: linear-gradient(135deg, #C9A84C, #E8C96D);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Billy footer wordmark */
      .billy-footer-mark {
        display: inline-flex;
        align-items: baseline;
        text-decoration: none;
        transition: opacity 0.2s;
      }
      .billy-footer-mark:hover { opacity: 0.8; }
      .billy-footer-text {
        font-family: 'Playfair Display', serif;
        font-weight: 700;
        font-size: 16px;
        color: #FAF7F2;
        letter-spacing: -0.01em;
      }
      .billy-footer-dot {
        font-family: 'Playfair Display', serif;
        font-weight: 700;
        font-size: 16px;
        background: linear-gradient(135deg, #C9A84C, #E8C96D);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }
    </style>
  </head>
  <body class="bg-brand-black text-white font-body antialiased">
    <slot />
  </body>
</html>
